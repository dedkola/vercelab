{
  "$schema": "https://unpkg.com/knip@6/schema.json",
  // The terminal server starts through the dev/start shell commands.
  "entry": ["server/terminal-ws.mjs!"],
  // Next.js resolves this marker to its bundled server-only implementation.
  "ignoreDependencies": ["server-only"],
}
